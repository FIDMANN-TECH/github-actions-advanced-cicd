{
  "name": "marketpeak-render-deploy",
  "version": "1.0.0",
  "description": "Demo Node app + static frontend for Render + CI/CD",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "lint": "echo \"no linter configured\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
